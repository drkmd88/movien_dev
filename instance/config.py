# This config file is used to save configures that are used privately,
# or configures that are confidential, say, keys, passwords.
#
# Use app.config.from_pyfile() to call this config file
# call this config file after call root config file:
# app.config.from_object() will over write root config file.

# Set Debug mode on
DEBUG = True
